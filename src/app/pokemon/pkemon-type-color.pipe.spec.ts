import { PkemonTypeColorPipe } from './pkemon-type-color.pipe';

describe('PkemonTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PkemonTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
